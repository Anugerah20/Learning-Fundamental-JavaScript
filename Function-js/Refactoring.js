 // Refactoring adalah proses mengubah kode menjadi sederhana tanpa mengubah fungsionalitasnya.

 function jumlahVolumeDuaKubus(a,b) {
    return Math.pow(a,3) + Math.pow(b,3);
}
document.writeln(`Jumlah Volume Dua Kubus Adalah ${jumlahVolumeDuaKubus(8,3)}`);